[build]
target = "index.html"
dist = "dist"

[watch]
ignore = ["dist", "target"]

[serve]
address = "127.0.0.1"
port = 8080
open = true

[[hooks]]
stage = "build"
command = "sh"
command_arguments = ["-c", "echo 'Building WASM...'"]

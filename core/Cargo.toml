[package]
name = "game-core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
glam = { workspace = true }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "combat"
harness = false

[[bench]]
name = "game_tick"
harness = false

[lints]
workspace = true
